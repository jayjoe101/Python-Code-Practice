s = "hello"
letter_h = s[0] # pulling a string from a string is called "Subscripting"

x = 123_423_021 # python will interpret this without the underscores
                # this is so its easier for us to read large numbers

type(x)         # gives the type of a variable 

y = x / 2       # division will always save as a floating value
z = 3 ** 2      # double ** means num to the power of num (exponent operator)

# PEMDAS = parentheses exponents mult/div add/subtract (from left to right)

l = round(3 / 12, 2) # round to two decimal places
l = 3 // 12     # forces the output to become an integer and not a float